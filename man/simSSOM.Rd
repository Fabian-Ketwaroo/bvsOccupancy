% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{simSSOM}
\alias{simSSOM}
\title{Simulate from a single-season occupancy model}
\usage{
simSSOM(
  M = 500,
  J = 5,
  n.cont = 3,
  n.cat = 1,
  n.levels = 3,
  beta.psi = NULL,
  beta.p = NULL,
  Jindex = NULL,
  seed = NULL
)
}
\arguments{
\item{M}{Number of sites.}

\item{J}{Maximum number of visits per site.}

\item{n.cont}{Number of continuous covariates in each component.}

\item{n.cat}{Number of categorical covariates in each component.}

\item{n.levels}{Number of levels of each categorical covariate.}

\item{beta.psi, beta.p}{Coefficient vectors, including the intercept. Default
to a sparse pattern built from the design.}

\item{Jindex}{Optional integer vector of length \code{M} giving the number of
visits actually made to each site, for simulating unequal replication.}

\item{seed}{Optional random seed.}
}
\value{
A list with the detection history \code{y}, the true occupancy state
  \code{z}, the design objects \code{occ.design} and \code{det.design}, the
  true coefficients, and the covariate data frames.
}
\description{
Generates site-level occupancy covariates and visit-level detection
covariates, a mixture of continuous and categorical, and simulates the
latent occupancy state and the detection history.
}
\details{
By default some covariates are given a coefficient of exactly zero, so that
variable selection has something to find. A categorical covariate is either
wholly present or wholly absent, matching the way the sampler treats it.
}
\examples{
sim <- simSSOM(M = 100, J = 3, seed = 1)
str(sim$y)
sim$beta.psi

}
