% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{simMSOM}
\alias{simMSOM}
\title{Simulate from a multi-species occupancy model}
\usage{
simMSOM(
  N = 20,
  S = 250,
  nvisits = 5,
  n.cont = 3,
  n.cat = 1,
  n.levels = 3,
  mu.z = NULL,
  tau.z = NULL,
  mu.p = NULL,
  tau.p = NULL,
  Trep = NULL,
  seed = NULL
)
}
\arguments{
\item{N}{Number of species.}

\item{S}{Number of sites.}

\item{nvisits}{Maximum number of visits per site.}

\item{n.cont}{Number of continuous covariates in each component.}

\item{n.cat}{Number of categorical covariates in each component.}

\item{n.levels}{Number of levels of each categorical covariate.}

\item{mu.z, mu.p}{Community means, one per covariate group including the
intercept. Default to a sparse pattern.}

\item{tau.z, tau.p}{Community variances, one per covariate group. Default to
a moderate variance for active covariates and a negligible one for the
rest.}

\item{Trep}{Optional integer vector of length \code{S} giving the number of
visits actually made to each site.}

\item{seed}{Optional random seed.}
}
\value{
A list with the detection history \code{y}, the true occupancy state
  \code{z}, the design objects, and the true species-level and
  community-level parameters.
}
\description{
Draws species-level coefficients from community-level distributions, using
the same block covariance as the fitted model, then simulates occupancy and
detection for each species.
}
\details{
Covariates whose community mean is zero are also given a very small
community variance, so that they are genuinely absent for every species.
This gives variable selection a clean target.
}
\examples{
sim <- simMSOM(N = 5, S = 80, nvisits = 3, seed = 1)
dim(sim$y)
sim$mu.z

}
