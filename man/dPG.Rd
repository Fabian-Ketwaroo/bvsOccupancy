% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nimble-helpers.R
\name{dPG}
\alias{dPG}
\alias{rPG}
\title{Placeholder density for Polya-Gamma latent variables}
\usage{
dPG(x = double(1), log = integer(0, default = 1))

rPG(n = integer(0))
}
\arguments{
\item{x}{Vector of latent variables.}

\item{log}{Logical (as an integer); return the value on the log scale.}

\item{n}{Number of draws; required by \pkg{nimble} but unused.}
}
\value{
\code{dPG} returns \code{0} (or \code{1} when \code{log = FALSE}).
}
\description{
The Polya-Gamma latent variables are updated in closed form by dedicated
samplers (\code{\link{PG_sampler_psi}} and friends), so the model only
needs a distribution that contributes a constant to the log posterior.
\code{dPG} returns zero on the log scale, and \code{rPG} deliberately
stops if it is ever called.
}
\details{
The distribution is registered with \pkg{nimble} when \pkg{bvsOccupancy}
is loaded, which is what allows model code to contain
\code{Omega[1:n] ~ dPG()}.
}
